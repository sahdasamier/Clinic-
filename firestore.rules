rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 🔒 Users collection - Allow authenticated users to read and write
    match /users/{userId} {
      // Allow users to read/write their own document
      allow read, write: if request.auth != null && request.auth.uid == userId;
      // Allow all authenticated users to read user documents (for admin functions)
      allow read: if request.auth != null;
      // Allow authenticated users to create user documents (for admin user creation)
      allow create: if request.auth != null;
    }
    
    // 🔒 Patients collection - Allow authenticated users
    match /patients/{patientId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Appointments collection - Allow authenticated users
    match /appointments/{appointmentId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Doctor-Patient Assignments - Allow authenticated users
    match /doctor_patient_assignments/{assignmentId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Medical Records - Allow authenticated users
    match /medicalRecords/{recordId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Inventory collection - Allow authenticated users
    match /inventory/{inventoryId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Payments collection - Allow authenticated users  
    match /payments/{paymentId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Clinics collection - Allow ALL authenticated users to read any clinic
    match /clinics/{clinicId} {
      allow read: if request.auth != null;
      allow write, create: if request.auth != null;
    }
    
    // 🔒 User invitations - Allow authenticated users
    match /user_invitations/{invitationId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Invitations - Allow authenticated users
    match /invitations/{invitationId} {
      allow read, write, create: if request.auth != null;
    }
    
    // 🔒 Any other collections - Allow authenticated users
    match /{document=**} {
      allow read, write, create: if request.auth != null;
    }
  }
} 